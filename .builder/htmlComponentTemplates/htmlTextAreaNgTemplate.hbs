<textarea 
   {{#if (hasValue class)}}class="{{{class}}}"{{/if}}
   {{#if (hasValue placeholder)}}placeholder="{{{placeholder}}}"{{/if}}
   {{#if (hasValue rows)}}rows="{{rows}}"{{/if}}
   {{#each custom}} {{name}}="{{{value}}}" {{/each}}>
     {{{value}}}
</textarea>