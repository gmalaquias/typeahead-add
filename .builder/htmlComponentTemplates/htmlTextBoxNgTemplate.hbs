<input 
     type="{{type}}"
     {{#if (hasValue class)}}class="{{{class}}}"{{/if}}
     {{#if (hasValue value)}}value="{{{value}}}"{{/if}}
     {{#if (hasValue class)}}placeholder="{{{placeholder}}}"{{/if}}
     {{#each custom}} {{name}}="{{{value}}}" {{/each}} />