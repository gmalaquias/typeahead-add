<a 
    {{#if (hasValue href)}}href="{{{href}}}"{{/if}}
	{{#if (hasValue target)}}target="{{{target}}}"{{/if}}
	{{#if (hasValue class)}}class="{{{class}}}"{{/if}}
	{{#each custom}} {{name}}="{{{value}}}" {{/each}}>
		{{{value}}}
</a>